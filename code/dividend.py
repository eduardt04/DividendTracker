import tkinter as tk

def show_dividend_info(window, stock_entry):
    print(stock_entry)
    stock_name = stock_entry.get()
    # Perform the logic to fetch dividend information for the stock name entered
    # Display the dividend information in a dialog box or update a label on the screen

    # Create a label and entry for entering the stock name
    # label = tk.Label(window, text=f"Regarding the stock: {stock_name}", font=("Helvetica", 14),
    #                  background="purple", foreground="black")
    # label.pack(pady=20)